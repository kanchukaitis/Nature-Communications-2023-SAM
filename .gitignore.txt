# Ignore all .nc files
*.nc

# Ignore all .zip files
*.zip

# Allow directories (necessary for Git to track subdirectories with allowed files)
!*/

# Standard exclusions for typical development environments
# Ignore logs, temporary files, and OS-specific files
*.log
*.tmp
.DS_Store
Thumbs.db

# Optionally, ignore specific directories containing these files
# If not needed, remove the following lines:
**/*.nc
**/*.zip
