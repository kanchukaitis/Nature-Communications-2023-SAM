function[latitudes] = tests
%% parameters.latitudes.tests  Return the latitude cutoffs to test
% ----------
%   latitudes = parameters.latitudes.tests
%   Returns the latitudes to test as cutoff thresholds.
% ----------
%   Outputs:
%       latitudes (numeric vector): The latitude cutoff thresholds to test
%           in the DA

latitudes = -50:30;

end